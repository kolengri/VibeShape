import { createAutomationReviewController } from "./automation-review-state"

// The browser document controller owns one active project and one confirmation surface.
export const automationReviews = createAutomationReviewController()
